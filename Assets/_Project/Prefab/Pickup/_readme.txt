This is place to store pickup prefab  
Make sure in the root 2D collider and Pickup.cs attached as component  
In the children of the root we can put a 2d representation (sprite )or 3d representation (mesh) for it  
For easier creation just duplicate one of this prefab and modifiy as per need  